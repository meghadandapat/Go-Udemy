package main

import "fmt" //main package can use fmt package

func main() {
	fmt.Println("Hey there")
}

// compile and execute project -> go run main.go
// compile -> go build
// formats -> go fmt
// installs pkgs -> go install
// runs and tests -> go tests

//pckages
//executable -> package main, builds an executable on running go build, contains a main() func
//reusable -> depency type package (any other name other than main)
